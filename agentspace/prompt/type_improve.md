# Type Improve

## Task 1 计划指定
深入分析当前项目中滥用 `any` 类型的问题。制定修复计划。

## Task 2 执行
执行 `agentspace/output/type-improve-plan.md`。
注意：
1. 每完成一个阶段后就要执行一次 type check，确保修改没有问题。
2. 全部完成之后，使用 `npm test` 确保测试仍然全部通过。