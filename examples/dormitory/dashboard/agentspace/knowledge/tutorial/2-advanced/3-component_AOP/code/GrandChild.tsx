/* @jsx createElement */
import {RenderContext} from "axii";

export function GrandChild({}, {createElement}: RenderContext) {
    return (
        <div as="root">
            this is grand child
        </div>
    )
}
