# application-key-occupancy — 运行记录

generated-by: cyclic-task-prompt | template-sha256: 6cfe19e4e81c | generated-at: 2026-08-16 | task-revised: 2026-08-17（首轮实施整体撤销后重启运行）

| 轮次 | 角色 | 结论 | 类别/证据 | 下一步 |
|------|------|------|-----------|--------|
| d0 | Task 1 设计初稿 | 缺口成立（要求 1 裁定）：声明式创建路径在应用键冲突时是不可重试故障+整体回滚（含交互事件零留痕）；最接近的 Custom+lockRows 备选 10/10 轮收敛但依赖框架未承诺的重试时序与 PostgreSQL SSI 方言行为，且正确性前提在应用代码中，不可认定为官方路径。方案：`Entity.identity`（声明式应用身份：全定+唯一+不可变+全路径集合语义创建），登记/消费/保留/观察复用 Transform、StateMachine（computeTarget 身份键查找，HEAD 已验证）、Entity.retention、effects+查询约定；零新增响应通道、零平行写 API。里程碑 4 个（M-01 内核 → M-02 并发登记 → M-03 一次性消费 → M-04 可见/保留/迁移/教义），设计完成时 N=20。 | 基线 git `c358cf9`（v4.9.0，工作树净）；基线套件 dataConstraints 14 / dispatchIdempotency 13 / entityRetention 15 全绿，`npm run check` 通过；探针 P1/P2（PGLite）、P3/P4（真实 PG 17.6 双连接+连接池双拓扑）4 文件 5 用例全绿（`agentspace/output/application-key-occupancy-probes/`）；P3 败者 `ConstraintViolationError` rawCode 23505 类型化+整体回滚；P3.3 消费恰好一次（A=1/B=0）；P4 顺序与竞态均收敛（`success-with-insert`+`success-no-insert`）；画像陈旧项修正：`assertNoIdInTransformedRecord` 已于 `fcfe2b9` 移除。 | additional task 1 设计评审 |
| d1 | additional task 2 设计裁决 | 评审结论「需要修订」；采纳 R-1（类别 1 关键事实错误）。Identity 改为记录元数据 + 专用 `createUniqueIndexSQL` / 列级 NOT NULL，禁止编译进 UniqueConstraint / NonNullConstraint；逻辑创建合同改为 createRecord 两步（确认插入才发 create 事件、空 RETURNING 读回既有行）；合表完成态为 setup fail-fast（删除 R-2 NULL 共存成功细胞）；补全 clone / public / manifest 读者。未驳回评审项。d=1/15，仍为设计中。 | 独立复跑 P1/P2 3 例、P3/P4 2 例全绿；Adj-R1 4 例：SQLite NonNullConstraint setup 失败、createTableSQL 忽略 notNull、ON CONFLICT DO NOTHING 时 `database.insert` 返回 undefined、UniqueConstraint 控制组仍抛 ConstraintViolationError。源码核对：`CreationExecutor.ts:436-444` 事件早于 INSERT；`SchemaDialect.ts:241-260` NonNull 为 CHECK；`SQLite.ts:101` nonNull:false；逻辑 id / Transform 索引已绕开 UniqueConstraint Klass。 | additional task 1 设计评审 |
| d2 | additional task 2 设计裁决 | 评审结论「通过」；未采纳任何「需要复审的问题」（评审列出 0 项）。独立复核六类复审条件均未命中：缺口求证仍成立（P1/P3 故障回滚；P4 收敛不可承诺）；`Entity.identity` 集合语义 + 既有 Transform/StateMachine/retention 与 Task 架构前提一致；ON CONFLICT 指定 arbiter 在真实 PG READ COMMITTED 双连接上 10/10 轮恰好一行、零 23505（review-at1）。方案不变。将评审实现注意事项核验后写入 §3.3 第 9–12 条、§3.3.1、§3.9 与 M-01 验收（返回既有行载荷、观察副作用清单、基记录解析、列级 NOT NULL 范围、mysql-like S2、taboo 合表隔离）；真值表补「核销时无行」行。status=实现中，N=5×4=20。 | HEAD `c358cf9`；复跑 P1/P2/Adj-R1 7 例、P3/P4 2 例、review-at1 3 例全绿。源码抽查：`CreationExecutor.ts:163-195` 依赖/unlink 早于 INSERT、`:436-444` 事件早于 INSERT、`:627` Object.assign；`createTableSQL` 忽略 notNull；`lockRecord` 无行返回 undefined（`MonoSystem.ts:1696`）；`isRetryableTransactionError` 不含 23505；`effects` 按 dispatch ALS 隔离（`Controller.ts:1579-1704`）；Transform insert 补丁不强制 SERIALIZABLE（`Scheduler.ts:883-887`）；`NewRecordData.ts:72-74` 已解析到基记录；retention 走 `storage.delete` 物理删行。mysql-like 夹具先例：`tests/runtime/dataConstraints.spec.ts:400-411` 与 `tests/runtime/entityIdentity.spec.ts:301`。 | additional task 3 实现 M-01 |
| k=1 | additional task 3 实现 | M-01 → 待审；`implementation-round: 1/20`；`next-action: 无`。实施 `Entity.identity`：声明守卫 D1–D6、列级 NOT NULL + 专用唯一索引、逻辑创建集合语义、身份列不可变、S1/S2 fail-fast。设计事实修正：迁移签名用 `records[].applicationIdentity`，不得覆盖 `records[].identity`（MigrationIdentity）。 | `applicationIdentity.spec.ts` 41 passed；`declarationTabooFuzz` 78 passed；writePathStructuralFuzz `FUZZ_SEED_START=100 FUZZ_SEED_COUNT=100 FUZZ_OPS=40` 108 passed；基线 dataConstraints 14 / dispatchIdempotency 13 / entityRetention 15 / entityIdentity 16 passed（2 skipped）；`npm run check` 通过。 | additional task 4 审计 M-01 |
| k=1 审计 | additional task 4 审计 | M-01 待审 → 已完成；无实现缺陷、无 reopen。验证缺口 V1–V8 由审计轮加强验收并注入无目标 ON CONFLICT 检验 arbiter 判别力后还原。`current-milestone: M-02`；`next-action: 无`。 | 复验 applicationIdentity 49 passed、taboo 78、writePathStructuralFuzz 108、基线 58 passed / 2 skipped、`npm run check` 通过；注入下 arbiter 两例变红后已还原。 | additional task 3 实现 M-02 |
| k=2 | additional task 3 实现 | M-02 → 待审；`implementation-round: 2/20`；`next-action: 无`。新增 `tests/runtime/postgresqlApplicationIdentity.spec.ts`：真实 PG 双拓扑并发登记（各 10 轮）+ 裸 INSERT 对照（败者 `ConstraintViolationError` rawCode 23505、交互事件回滚）。纳入 `test:postgres`。 | 真 PG 17：3 passed（600ms）；无 env：3 skipped；`npm run check` 通过。生产写路径未改。 | additional task 4 审计 M-02 |
| k=2 审计 | additional task 4 审计 | M-02 待审 → 已完成；无实现缺陷、无 reopen。验证缺口 V1–V3（强制重叠、effects 按 dispatch 归属、副本 B 查询）由审计轮加强验收；去掉 ON CONFLICT 后拓扑 a/b 均红，已还原。`current-milestone: M-03`；`next-action: 无`。 | 复验 postgresqlApplicationIdentity 3 passed（加强后）/ 无 env 3 skipped；applicationIdentity 49；基线 42；`npm run check` 通过。 | additional task 3 实现 M-03 |
| k=3 | additional task 3 实现 | M-03 → 待审；`implementation-round: 3/20`；`next-action: 无`。官方配方接到 identity 实体：Transform 登记 + status/consumedBy StateMachine 消费；§3.4 真值表可编程分类；真实 PG 双拓扑并发消费（`lockRecord` 栅栏强制重叠）。生产写路径未改。 | applicationIdentity 50 passed；postgresqlApplicationIdentity 6 passed（真 PG 17）/ 无 env 6 skipped；基线 dataConstraints 14 / dispatchIdempotency 13 / entityRetention 15；`npm run check` 通过。 | additional task 4 审计 M-03 |
| k=3 审计 | additional task 4 审计 | M-03 待审 → 已完成；无实现缺陷、无 reopen。验证缺口 V1–V3（胜者 update 必须含 status/consumedBy、同 nonce 再核销不二次转移、PG 顺序已使用钉住第一核销方）由审计轮加强验收；去掉 lockRecord 的 FOR UPDATE 后拓扑 a/b 均红，已还原。`current-milestone: M-04`；`next-action: 无`。 | 复验 applicationIdentity 50 passed、postgresqlApplicationIdentity 6 passed（加强后）/ 无 env 6 skipped、基线 42、`npm run check` 通过。 | additional task 3 实现 M-04 |
| k=4 | additional task 3 实现 | M-04 → 待审；`implementation-round: 4/20`；`next-action: 无`。身份进入 `records[].applicationIdentity` 与 modelHash；既有表新增 identity 为 additive 唯一索引 + 迁移期 NULL/重复键校验；retention.ttl 回收后同键重登记；usage/generator/CHANGELOG 官方配方与 CREATE TABLE 反模式。 | applicationIdentityMigration 8 passed；entityRetention 15；migrationGenerativeFuzz 默认池 7；applicationIdentity 50；UniqueConstraint/NonNull 迁移 3；基线 dataConstraints 14 / dispatchIdempotency 13；`npm run check` / `npm run build` 通过。 | additional task 4 审计 M-04 |
| k=4 审计 | additional task 4 审计 | M-04 待审 → 已完成；当时结论无实现缺陷。随后被独立写路径复验推翻，见下一行。 | 当时复验 applicationIdentityMigration 9、entityRetention 15、migrationGenerativeFuzz 7、applicationIdentity 50（尚无 D-1 复现）。 | 当时终止；已撤回 |
| 独立审计 | additional task 4 | 撤回终止。M-01 已完成→开放；reopen-count 1；领域 `filtered-membership-events`。identity 插入在 FK 写入之后采集相关记录成员资格快照：1:1 反向 filtered 查询正确、create 事件缺失。k 仍为 4/20。 | 失败：`npx vitest run tests/runtime/applicationIdentity.spec.ts -t "1:1 peer filtered Count"`。对照无 identity 的同一拓扑 Count=1。验证加强：boolean/0 身份、1:1 非 reliance 合法孪生、taboo D4 merged。 | additional task 3：按 next-action 修正 `createIdentityRecord` 快照时序 |
| k=5 | additional task 3 实现 | M-01 → 待审；`implementation-round: 5/20`；`next-action: 无`。将 `createIdentityRecord` 的 `collectCreationLinkChecks` 移到 `patchIdentityHostRelations` / `handleCreationReliance` 之前（与 `createRecord` 同序）；观察路径仍早退。补 1:1 nested 兄弟格；维度登记表回填 identity 为 related-record 成员资格的兄弟产生点。 | `applicationIdentity.spec.ts` 当时 55 passed（D-1 Count 变绿）；`declarationTabooFuzz` 81；writePathStructuralFuzz 108；`postgresqlApplicationIdentity` 6（真 PG）；基线 dataConstraints 14 / dispatchIdempotency 13 / entityRetention 15。 | additional task 4 审计 M-01 |
| k=5 审计 | additional task 4 审计 | M-01 待审→开放；reopen-count 2；新领域 `identity-merged-link-record`。D-1 快照时序成立，但第二步补丁只用宿主 `isRecord` 过滤，丢掉 merged link id / `&` 列：实体反向路径可读，`findRelationByName` 为空，关系 create 事件成为幽灵，1:1 抢夺双 owner。 | `applicationIdentity.spec.ts` 8 failed / 53 passed（钉入关系行与 steal 后）；taboo 81；fuzz 108；真 PG 占用 6；基线 42。对照：identity 作 1:1 TARGET 与无 identity 1:1 关系行存在。 | additional task 3：按 next-action 写出完整 merged-link 同行 |
| k=6 | additional task 3 实现 | M-01 → 待审；`implementation-round: 6/20`；`next-action: 无`。`createIdentityRecord` 第二步按物理列写完整 merged-link 同行（FK + link id + `&`）；观察路径仍早退。补 `&` 与宿主 value 同名格。 | `applicationIdentity.spec.ts` 当时 62 passed；`declarationTabooFuzz` 81；writePathStructuralFuzz 108；`postgresqlApplicationIdentity` 6（真 PG）；基线 dataConstraints 14 / dispatchIdempotency 13 / entityRetention 15；`npm run check` 通过。 | additional task 4 审计 M-01 |
| k=6 审计 | additional task 4 审计 | M-01 待审→已完成；无实现缺陷、无 reopen。验证缺口：身份宿主 merged-link 有限领域一次列全（n:1 `&`、双 merged link、n:n、嵌套 identity 子记录、抢夺 delete 事件、过滤名+1:1、SQLite n:1、宿主 create 默认值与关系端点、真 PG merged-link）。注入 D-2 旧 `isRecord` 过滤器后 6 例变红，已还原。全部里程碑关闭；`status: 已完成`。 | 复验 applicationIdentity 71、taboo 81、fuzz 108、postgresqlApplicationIdentity 7、migration 9、migrationGenerativeFuzz 7、基线 42、`npm run check` / `npm run build` 通过。 | 终止 |

## 终止记录

- **终止状态**：已完成。原因：四个里程碑均经独立审计关闭；k=6 审计复验 M-01 无实现缺陷；最终核验全部里程碑验收命令与 Task 编号要求通过。
- **设计轮数**：d=2/15。**实现轮数**：k=6/20。
- **里程碑最终状态**：M-01 已完成（reopen-count 2）；M-02 / M-03 / M-04 已完成（reopen-count 0）。
- **设计阶段采纳的问题类别**：d1 采纳关键事实错误（R-1：identity 不得编译进 UniqueConstraint / NonNullConstraint）。d2 未采纳复审问题。
- **审计发现**：实现缺陷 2（D-1 `filtered-membership-events`，D-2 `identity-merged-link-record`，均在 M-01）；验证缺口若干（k=1 V1–V8、k=2 V1–V3、k=3 V1–V3、k=4 教义/迁移钉、k=6 merged-link 有限领域列全）。验证缺口均由审计轮加强后产品仍正确，未因此退回。
- **reopen / 收敛**：M-01 reopen 2 次、两个不同领域各 1 次；未触发 domain-review 或 milestone-review。M-01 关闭于 k=6 审计。M-02/03/04 无 reopen。
- **自动调整里程碑**：0。
- **人工介入**：0。
- **预算**：N=20，实际 k=6，满足。无未完成阻塞。
- **对协议的改进建议**（最多三项）：
  1. 身份逻辑创建作为 `createRecord` 的兄弟产生点时，第一个内核里程碑必须同时钉住「实体嵌套读取」与 `findRelationByName` / unlink 两个读者。本运行 M-01 先按实体反向路径关闭，D-2 在下一审计才出现（k=5 审计证据）。
  2. 同一验证领域连续两轮靠事后补格才暴露缺陷时，应立即按维度登记表把该产生点的有限拓扑/载荷/读者一次列全。k=5 补了 `findRelationByName`，k=6 才列全 n:1 `&`、双 link、抢夺事件面与真 PG（本轮审计）。
  3. 结构 fuzz 的生成域若不含新声明（此处为 `Entity.identity`），其绿结果不得写入该声明的写路径完成证明。本运行 fuzz 108 从未生成 identity，D-1/D-2 只能由定向套件抓住。

