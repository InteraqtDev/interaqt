# entity-identity-and-relations — 运行记录

generated-by: Task 1 design session
baseline-revision: b7fe969d5b3e5fcc6518c830ad9744e6246bf6bc
started: 2026-08-10

| 轮次 | 角色 | 摘要 |
|------|------|------|
| d0 | 设计 | 求证完成：问题成立（Transform 硬禁 id + 文档夹击 + 存储缺逻辑 id 唯一/update 可改写/INT 序列不在线推进）。最小实验 E1–E10（PGLite/SQLite）。方案：单一身份；insert 可选 id；update 不可变；框架逻辑 id UNIQUE；先存储不变量再放宽 Transform。里程碑 M-01..M-05 全开放。未写生产代码。 |
| d1 | 裁决 | 评审结论「需要修订」。采纳 R1（类别 1+5）：MySQL `unique:false` 使经 `UniqueConstraint`→`createUniqueConstraintStatement` 自动登记逻辑 id 唯一会使 setup 崩溃；复验 `dataConstraints` MySQL 探针 + `createUniqueIndexSQL` 可生成 id 索引。锁定框架专有 UNIQUE INDEX 路径；保持 MySQL 用户 UniqueConstraint fail-fast；更新 §3.3/§3.7/M-01/风险表。未驳回项。下一步：additional task 1 复审修订后设计。 |
| d1 | 评审 | 结论「需要修订」。独立探针复验主线 E2/E4/E5/E6/E8 与 d1 MySQL 路径成立。新 R1（类别 1+5）：逻辑 id 物理列是 `generateShortFieldName` 哈希名（如 `use_id_12wcb9i`），非字面 `id`；合表多 record 多逻辑 id 列；§3.3 示例 ``ON T (id)`` 会使 M-01 失效。要求按 `recordInfo.idField` per base record 建索引并写入 M-01 验收。未改设计。下一步：additional task 2。 |
| d2 | 裁决 | 评审结论「需要修订」。采纳唯一项 R1（类别 1+5，领域「框架逻辑 id UNIQUE INDEX 发出」第二次命中）。独立复验 E11：PGLite+SQLite 上 `idField` 为哈希短名；字面 `id` DDL → column does not exist；1:1 reliance 合表三逻辑 id；按各 `idField` 建索引后重复 create fail-loud。§3.3 改为完整可执行契约（枚举/列解析/路径/方言/命名/参考函数）；修正 F5/E10、M-01 验收、读者表、风险与非采用分支。未驳回项。`design-round: 2/15`。下一步：additional task 1 复审修订后设计。 |
| d2 | 评审 | 结论「通过」。独立源码+探针复验 F1/F3–F7/F9、E2/E4/E5/E6、物理 `idField`/合表三列、MySQL 专有 `createUniqueIndexSQL` 路径与参考函数枚举；六类复审条件无命中。实现注意事项（迁移挂点、`noteAllocatedId`、MySQL IF NOT EXISTS、内部表）不触发复审。未改设计。下一步：additional task 2。 |
| d3 | 裁决 | 评审文件仍载 d1 后 R1「需要修订」正文；对照**当前**设计独立复验后**不采纳**任何复审项（R1-field 已在 d2 闭合）。证据：源码 F1/F3/F4/F7/F9 + 探针 E2/E4/E5/E6/Transform/`idField`/合表三列/字面 id DDL 失败/idField UNIQUE fail-loud；MySQL `unique:false` + `createUniqueIndexSQL` 专有路径。设计通过 → `status: 实现中`，`implementation-round: 0/25`（M=5）。下一步：additional task 3 实施 M-01。 |
| k=1 | 实现 | M-01 存储身份不变量。`SchemaDialect.frameworkLogicalIdUniqueIndexes` + `createFrameworkLogicalIdUniqueIndexSQL`（per non-filtered × idField × `createUniqueIndexSQL`）；`MonoSystem` setup + migration postRecompute 发出；`CreationExecutor.preprocessSameRowData` update 固定 old id、create 外部 id 调 `noteAllocatedId`；SQLite/MySQL/PostgreSQL 实现推进发号。验收 `entityIdentity.spec.ts` 7 passed；`dataConstraints` 14 passed。M-01 → `待审`。下一步：additional task 4。 |
| k=1 | 审计 | 复验 entityIdentity 7 + dataConstraints 14 绿。对照设计：唯一/update 剥离/发号/idField 合表/mysql 用户约束主路径成立。**实现缺陷 D1**（领域「框架逻辑 id UNIQUE INDEX setup 幂等」）：setup 无条件 emit 框架 id UNIQUE，MySQL 无 IF NOT EXISTS → setup(false) 再入 Duplicate key name，破坏 mysqlIdSequenceReconcile 类 attach 与 M-01「setup 不因框架 id UNIQUE 崩溃」。验证缺口 V1（验收未钉再入）、V2（schema.constraints 未登记，非阻塞）。M-01 待审→开放，reopen-count=1。下一步：additional task 3 修 D1。 |
| k=2 | 实现 | 修 D1：`isIndexAlreadyExists` + setup 捕获再入；V1 验收钉 `setup(true)→setup(false)`（PGLite/SQLite）+ mysql-like 错误归一 + 可选真实 MySQL；V2 框架 id 索引进入 `schema.constraints`。`entityIdentity` 8 passed/1 skipped，`dataConstraints` 14，`check` 绿。M-01 → `待审`。下一步：additional task 4。 |
| k=2 | 审计 | 复验 entityIdentity（加强后 9 passed/1 skipped）+ dataConstraints 14。D1 修复成立：normalize 区分 1061/42P07 vs 1062/23505；setupFrameworkLogicalIdUniqueIndexes catch 幂等；审计轮补 catch-path 用例（无 IF NOT EXISTS stub）。无实现缺陷。M-01 待审→**已完成**。下一步：additional task 3 实施 M-02。 |
| k=3 | 实现 | M-02 Transform/applyResultPatch 规则收敛。删除 `assertNoIdInTransformedRecord`（insert 允许 id；唯一索引 fail-loud）；Transform data-based update 与 `Controller.applyResultPatch` 双层剥离 `data.id`。r13 F-3 改为碰撞唯一约束 + 合法 id insert + strip 正对照；`entityIdentity` 增 M-02 段（data/event insert、spread 共享、update 不改身份、applyResultPatch 剥离）。验收 34 passed/1 skipped；dataConstraints 14；check 绿。M-02 → `待审`。下一步：additional task 4。 |
| k=3 | 审计 | 复验 M-02 验收 34/1 skip + dataConstraints 14 + check。源码对照 §3.2/§3.4：守卫删除、insert 透传、update 双层剥离、delete/affectedId 成立。无实现缺陷。验证缺口 V4（Controller 剥离仅靠落库断言）/V5（r13 碰撞 toBeTruthy）审计轮直接加强后复验绿；缺陷注入可区分 Controller strip。M-02 待审→**已完成**。下一步：additional task 3 实施 M-03。 |
| k=4 | 实现 | M-03 预生成 id × Relation × computeTarget E2E。`entityIdentity` 增两用例：PGLite+SQLite 预生成 author/post → Relation `{id}` + nested attributeQuery 双向 + SM computeTarget 经 payload 同 id 改 status；SQLite INT 路径 payload 字符串化后仍定位。无生产代码改动。验收 16 passed/1 skipped；check 绿。M-03 → `待审`。下一步：additional task 4。 |
| k=4 | 审计 | 复验 entityIdentity 16/1 skip + r13/dataConstraints 23 + check。对照 §3.5 无实现缺陷。验证缺口 V6（单行 SM 不证明 computeTarget 按 id 选择）审计轮直接加强为双预生成 id + 兄弟行仍 pending 后复验绿。M-03 待审→**已完成**。下一步：additional task 3 实施 M-04。 |
| k=5 | 实现 | M-04 文档与知识库一致。改写 generator/computation-implementation、usage/04-reactive-computations、usage/19-common-anti-patterns §9、generator/api-reference create/update、AGENTS.md 测试约定；删除「永远禁止手写/返回 id」与「Why IDs Must Be Auto-Generated / clientId 唯一推荐」。验收 `rg` 零命中；无 src 变更。M-04 → `待审`。下一步：additional task 4。 |
| k=5 | 审计 | 复验 M-04：验收 `rg` 零命中；扩展绝对禁令扫描 agent/+AGENTS.md 空；§3.6 四锚点与 AGENTS.md 抽读与运行时一致；entityIdentity/r13/dataConstraints 39/1 skip + check 绿。无实现缺陷。M-04 待审→**已完成**。下一步：additional task 3 实施 M-05。 |
| k=6 | 实现 | M-05 回归与写路径加固。无新增 src 改动。验收：check；entityIdentity/r13/transform* 40 passed/1 skipped；writePathStructuralFuzz 默认+扩展 100–119；driverDifferential SQLite↔PGLite；migrationGenerativeFuzz；dataConstraints；真实 PG idConsistency/concurrency/dataConstraints 20 passed。M-05 → `待审`。下一步：additional task 4。 |
| k=6 | 审计 | 复验 M-05 全套验收绿（check；主套件 40/2 skip；fuzz/migration/dataConstraints；真实 PG 20）。对照设计与 Task 1–10：单一身份、Transform 规则、文档、兼容超集均成立；无实现缺陷、无 reopen。验证缺口 V7（PG `noteAllocatedId` 缺真实驱动钉）审计轮直接加强 entityIdentity PG 序列推进用例后复验 1 passed。M-05 待审→**已完成**。全部里程碑完成 → `status: 已完成`。终止，不启动新 chat。 |
