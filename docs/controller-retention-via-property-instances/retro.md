# 运行记录：controller-retention-via-property-instances Task 1

<!-- generated-by: cyclic-task-prompt | template-sha256: 43a33f632926 | generated-at: 2026-09-05 -->

任务文件：`docs/controller-retention-via-property-instances/controller-retention-via-property-instances-task.md`
基线 revision：`0249556`

## 轮次记录

- 设计轮 d=0（2026-09-05，初始设计）：在当前 HEAD 上以 `/tmp/interaqt-retention/repro.mts` 复现（PGLite 4 轮：每轮 `Entity+3 / Relation+3 / Property+9`，Controller / System / Database 全部存活，RSS 每轮 +≈215 MB；SQLite 对照注册表增量相同、三者同样存活）。因果实验：只截断 `Property.instances` 三者仍存活；同时截断 `Entity` / `Relation` 后三者全部释放。结论：问题存在，保留链两族（Property 直接链 + merged 派生 Entity / Relation 间接链），8 个运行时登记点，除注册表外无其它静态根。裁定方案：`Entity/Relation/Property.derive` 非注册构造为唯一派生路径；切断 bound-state `defaultValue` 闭包捕获；结束合同 = `system.destroy()` + 丢弃引用，不新增 API。基线：`npm run check`、core + 定向 spec（649 用例）、真实 PG 套件（48 用例）全部通过。里程碑 M-01..M-04，全部`开放`。下一步：启动设计评审（additional task 1）。
- 设计裁决轮 d=1（2026-09-05）：评审结论「通过」，采纳需要复审问题 **0** 类。裁决独立复核全部承重主张：复现脚本与两个因果实验亲自复跑（数字与设计 §1.3 一致：每轮 +3/+3/+9、四轮全存活；truncate-all 后三者全释放）；源码级确认 `Property.create` 登记、R1–R8、merged 守卫以 `inputRelations` 为条件、`HardDeletionProperty` 无运行时调用点、manifest 不读注册表、`BoolExp.atom`/`MatchExp.atom` 非注册路径；`npm run check` + 27 文件 559 用例通过。补充修正（不构成复审问题）：(a) `stringifyAllInstances` 测试读者实为 3 个文件；(b) 设计补充既有 `Controller.teardown()` 与 FR-RET-02 结束合同的场景区分（teardown 是 system 存活下替换 controller 的入口，因果实验 B 未调用它即全部回收）；(c) 采纳评审实现注意事项 2/3/4/5 为实现约束（WeakRef 形态、migration 轨两批计量、`typeof` 瑕疵不修复、allowlist 纳入 drivers）。设计通过，`status: 实现中`，`N = 5 × 4 = 20`。下一步：启动实现轮（additional task 3），从 M-01 开始。
- 实现轮 k=1（2026-09-05，M-01）：新增 `tests/runtime/derivedDefinitionRetention.spec.ts`（7 用例：注册表不变量 pglite/sqlite、可回收性、`stringifyAllInstances` 纯净性、R1–R8 同类位置、migration 轨分轨计量、闭包捕获断言）。当前 HEAD 上 **7/7 红**且数字与设计 §1.3 一致：install 轨每轮 `Entity+3/Relation+3/Property+9`、三 `WeakRef` 存活、migration 轨 `setup(false)` 增量 `+6/+6/+18`（两批）、5 个 bound-state 闭包均为 `() => stateItem.defaultValue`。`npm run check` 通过；同命令二次运行复现 7/7 红；与 `tests/core` 同跑无相互污染（546 用例全绿）。M-01 标记`待审`。实现期修正两处 spec 自身缺陷（不触生产代码）：Controller 构造参数 shorthand `{system2}` 导致 `options.system` 为 undefined（改为显式 `system:`）；`Entity.create` 无 `indexes` 字段（误写，删除；Transform 唯一索引由 record-based Transform 的 bound state `unique` 标志覆盖）。下一步：审计轮（additional task 4）。
- 审计轮 a=1（2026-09-05，M-01）：复验全部验收命令通过（7/7 红两次复现、`npm run check`、与 tests/core 同跑无污染），红数字与设计一致。对抗性审查（3 个 /tmp 探针：零 setup 注册表残留、单轮逐名增量、R8 触发形态）发现 **3 处实现缺陷（同域）**，M-01 `待审`→`开放`，reopen +1（`per-site 覆盖断言`）。（A）per-site 末尾全注册表身份清查不可满足：4 个内置实体（`_System_`/`_Dictionary_`/`_Interaction_`/`_Activity_`）+ `activityInteraction` 按设计在 import 期登记，任何修复后仍存在；（B）R3 断言按不存在的名字 `'AuthorCat'` 过滤（实名 `Author_likedPosts_likedBy_Post`），未修复与修复后同为 1 条、无判别力；（C）R8 恒空：`HotEngaged` 是 filtered-over-merged（不触发虚拟 base relation），触发条件是 filtered-input-into-merged（探针已演示 `__Engaged_base`）。已裁定设计 §3.5 第 4 项 R8 子句前提错误，属实现轮可修范围。同类检查：其余用例（注册表不变量、可回收性、纯净性、migration 轨、闭包捕获、R1/R2/R4–R7 条目）判别力与可满足性均合格；生产代码零改动。next-action 写明三项修正 + 按 R 站点拆用例（首断言失败遮蔽后续断言是缺陷 A 逃过自检的机制）。下一步：实现轮 k=2（additional task 3）。
- 实现轮 k=2（2026-09-05，M-01 修订）：按审计 next-action 完成三项修正（均只动 spec，生产代码零改动）：（A）全注册表清查改为 import 期身份快照 sweep（快照天然含框架内置声明，不硬编码名单）；（B）R3 改按自动生成名（`Author_likedPosts_likedBy_Post` / `Author_hotLikedPosts_hotLikedBy_Post` / `Author_sharedPosts_sharedBy_Post`）计数 + `===` 用户实例；（C）schema 扩展为 filtered-input-into-merged 触发 R8（`HotAuthorCat` filtered 视图作 merged input，plain inputs 增 `weight` 属性），并同步修正设计 §3.5 第 4 项 R8 前提与红色基线数字（install 轨 Relation 每轮 +5；migration 轨 +6/+10/+18 与 +9/+15/+27）。per-site 拆为 6 个独立用例 + 独立 sweep 用例（共 13 用例）。验收：**13/13 红**且三次复跑一致；R3 隔离运行 2 条、R8 隔离运行 1 条（均可判别）；失败输出中内置名（`_System_` 等 5 项）出现 0 次；`npm run check` 通过；与 tests/core 同跑 546 用例全绿无污染。M-01 重新标记`待审`。下一步：审计轮（additional task 4）。
- 审计轮 a=2（2026-09-05，M-01）：复验全部验收命令通过（13/13 红两次复现、全部 per-site/sweep 隔离即红、`npm run check`、与 tests/core 同跑无污染），红数字与设计修订后基线逐项自洽；k=1 三项缺陷（A 不可满足清查 / B R3 无判别力 / C R8 恒空）逐项独立复验闭合。可满足性探针：两轮逐名增量完全重复（无懒加载一次性登记，修复后零增长可满足）；import 快照截断模拟修复后 `stringifyAllInstances` 前后逐字节相等；migration 轨隔离计量与 spec 预期一致。发现 **1 处验证缺口（产品实现未被证明错误）**：per-site/sweep 断言原先只在 `destroy()` 后观察，「setup 期登记 + destroy 期反注册」的截断式伪修复可通过全部形态断言（注入实验证实）；审计轮按协议直接加强——`oneRound` 增 `observe` 回调，证据捕获移至 setup 后、destroy 前，注入复验伪修复下 R1/R2=6、R3=2、sweep 含 3 派生实体全部抓红，未修复 HEAD 仍 13/13 红且计数不变，check 通过。无实现缺陷，纯验证缺口不触发 reopen（reopen-count 仍 1，normal）。**M-01 `待审`→`已完成`**。下一步：实现轮 k=3（additional task 3，进入 M-02）。
- 实现轮 k=3（2026-09-05，M-02）：`Entity` / `Relation` / `Property` 三个 Klass 新增 `static derive(args)`——校验块原样提取为私有 `validateCreateArgs`（Entity 版返回规范化的 `{retention, identity}`），`create = validate + new + uuid 去重登记`、`derive = validate + new`（无 options、新 uuid、不登记）；`create()` 行为与错误信息逐字不变（17 组「derive 与 create 错误信息一致」判例 + declarationTabooFuzz 81 用例背书）。新增 `tests/core/deriveDefinition.spec.ts`（35 用例全绿：不登记、`stringifyAllInstances` 纯净、create 仍登记对照、uuid 新、clone / RefContainer constructor / add / replace 对 derive 实例的身份合同）与 `tests/core/runtimeCreateSiteAllowlist.spec.ts`（AST 扫描 src/runtime|storage|builtins|drivers 的 `X.create(` 调用点，允许清单 = System.ts / Interaction.ts / ActivityManager.ts / Controller.ts(HardDeletionProperty)；本里程碑**预期红**，失败输出恰为 R1–R8 8 处）。验收：tests/core 583 绿 + allowlist 1 红（唯一失败，8 处与 §1.2 逐项一致）；`npm run check` 通过。影响面（create() 内部重构）：`tests/storage` 全量 846 绿、migration 相关 + r12/r23 review 163 绿、`npm test` 全量仅 2 个设计内红（M-01 13 用例 + allowlist 1 用例）其余 2755 绿；M-01 红数字不变（install 每轮 +3/+5/+9）。实现轮内设计订正 1 处：M-02 验收「tests/core 全绿」与「allowlist 红」措辞矛盾，订正为「除 allowlist 外全绿」。M-02 标记`待审`。下一步：审计轮（additional task 4）。

- 审计轮 a=3（2026-09-05，M-02）：复验全部验收命令通过（tests/core 583 绿 + allowlist 1 红 = R1–R8 恰 8 处、deriveDefinition 35/35、check、taboo 81、migration/review 163、storage 846、npm test 全量仅 2 文件 14 用例设计内红、真实 PG 48 全绿）。对抗性复核：三文件 validateCreateArgs 与 HEAD 逐行对照零差异；generateUUID/parse/RecordQuery.derive 命名各点核实。发现 **1 处验证缺口（产品实现未被证明错误）**：allowlist 允许判定是文件成员资格而非设计指定的站点结构，注入实验（Controller.teardown 体内注入 create + 源码级模拟 M-03 态）证实注入站点静默通过；审计轮按协议直接加强——改为站点级结构谓词（顶层初始化器 / HardDeletionProperty 工厂）+ 收紧断言（恰好四文件、恰 1 工厂站点），四格判别矩阵复验（M-02 红 8 处不变、注入被抓红、模拟 M-03+注入红恰 1 处、模拟 M-03 干净绿），check 通过。附带发现：Scheduler.ts:106–219 AsyncTask 合成走 raw new（不登记、非登记点），记为 M-03 注意事项不迁移。无实现缺陷，纯验证缺口不触发 reopen（reopen-count 仍 0，normal）。**M-02 `待审`→`已完成`**。下一步：实现轮 k=4（additional task 3，进入 M-03）。
- 实现轮 k=4（2026-09-05，M-03）：R1–R8 全部改走 `derive`——`MonoSystem.setup()` 删除与 `prepareEntitiesForStorage()` 逐字同构的 state 注入循环、改为调用后者（install 轨与 migration 轨汇合为单一编译入口），bound-state 属性用 `Property.derive` 且 `defaultValue` 闭包只捕获纯值（不再捕获 `stateItem`→controller）；`MergedItemProcessor` 的 R3/R4/R5/R6/R7/R8 分别改 `Relation.derive` / `Property.derive` / `Entity.derive` ×2 / `Relation.derive` ×2；`Scheduler.ts` AsyncTask 合成（raw new，非登记点）按审计注意事项未迁移。验收全部通过：M-01 spec **13/13 红转绿** + `tests/core` 全绿（allowlist 红转绿，运行时 create 站点仅剩 4 个内置声明文件顶层 + HardDeletionProperty 工厂）；`tests/storage` 846 绿；migration+review 六套件 163 绿；真实 PG 48 绿；`npm run check` 通过；`npm test` 全量 2769 绿 / 66 skip（k=3 的 14 个设计内红全部转绿，无新增失败）；佐证 `/tmp` 复现脚本 none 模式每轮 `delta={}`、四轮三 `WeakRef` 全释放、RSS 平稳（402→411 MB，修复前每轮 +≈215 MB）。M-03 标记`待审`。下一步：审计轮（additional task 4）。
- 审计轮 a=4（2026-09-05，M-03）：复验全部验收命令通过（597 = M-01 spec 13 红转绿 + tests/core 含 allowlist 转绿、storage 846、migration+review 163、真实 PG 48、check、干净树 `npm test` 2769 绿 0 红；/tmp 佐证每轮 delta={}、三 WeakRef 全释放、RSS 平稳）。对抗性审查：diff 与设计 §3.2/§3.3 逐项一致（汇合返回值与旧 setup 展开逐字相同、derive 静态调用点恰 7 处、create 重复 uuid 抛错故无身份差异、AsyncTask raw new 维持范围外）；缺陷注入检验：R1/R2 与 R4 站点 derive→create 各被 6 用例抓红，注入后已恢复（工作树回 k=4 结束态）。工作树观察：prompt/skill/ 两文件改动为任务协议自身会话启动工具，非生产改动。无实现缺陷、无验证缺口。**M-03 `待审`→`已完成`**。下一步：实现轮 k=5（additional task 3，进入 M-04）。
- 实现轮 k=5（2026-09-05，M-04）：落地四份文档交付物——(1) 新建 `agentspace/knowledge/controller-lifecycle-and-declaration-registry.md`（注册表设计对象与读者、`derive` 汇合点路径、结束合同 5 条保证、`teardown()` 与整图结束的场景对照、两族保留链因果证据）；(2) `agent/agentspace/knowledge/usage/13-testing.md` 新增「Controller lifecycle and cleanup」小节（`clearAllInstances` 非清理入口、teardown 场景区分）；(3) `CHANGELOG.md` `[Unreleased]` 三段（Bug Fixes 行为变化 + Features `derive` 注明框架内部路径 + Docs）；(4) `tests/runtime/WritingComputationTests.md` 维度登记册新增「运行时派生定义 × 登记面 × 持有 Controller 图」行（含观察时点判别维度与间接链历史逃逸）。验收全部通过：`npm run build` 通过且 `static derive` 进入三个 d.ts（`dist/core/Entity.d.ts:186` 等）；两处 `rg` 文档检查命中；`npm test` 全量 234 文件 / 2769 通过 / 66 skip，与 a=4 基线一致（零新增失败）。M-04 标记`待审`。下一步：审计轮（additional task 4）。
- 审计轮 a=5（2026-09-05，M-04）：复验全部验收命令通过（build + derive 入三个 d.ts、rg 文档检查 11/6/24/1、npm test 全量 2769 绿 0 失败与 k=5 一致、受影响里程碑定向 51 用例绿、M-04 零 src 改动）。对抗性事实核查（destroy 链、getBoundStateName、teardown、注册表读者枚举逐条对照代码）发现 **1 处实现缺陷**：知识文档 §1 把 migration manifest 列为 `stringifyAllInstances()` 产物的回读消费者，与代码（`migration.ts:1010` createMigrationManifest 读 controller 图 + storage schema，注册表零引用）、设计 §1.1、同轮 CHANGELOG:28 三重矛盾；同类检查三文档 + 维度行仅此 1 处命中。M-04 `待审`→`开放`，reopen +1（`文档事实准确性`），normal 不变。next-action 写明修正句、验证方式与复跑命令。下一步：实现轮 k=6（additional task 3）。
- 实现轮 k=6（2026-09-05，M-04 修正）：按 a=5 缺陷 D1 修正知识文档 §1 读者枚举——`stringifyAllInstances()` 产物供 `parse`（`createInstancesFromString`）回读；migration manifest 不是其消费者（`createMigrationManifest` 读 controller 图 `entities/relations/dict/scheduler.computationsHandles` 与 storage schema，源码逐字段核实），并保留该事实作为「派生定义移出输出不影响 manifest」的依据；合同 §3 五条保证与 teardown 对照表未动（审计注意事项 1）。三方一致性复验通过（知识文档表述 ↔ `migration.ts` 注册表零引用 ↔ CHANGELOG:28）；同类扫描（两文档全部读者/消费者表述）无残留。M-04 验收命令复跑全通过：`npm run build`（derive 仍在三个 d.ts）、两处 rg（11/6/24/1 不变）、`npm test` 全量 234 文件 / 2769 通过 / 66 skip / 0 失败（与 k=5 / a=4 基线一致）、`npm run check`。M-04 重新标记`待审`。下一步：审计轮（additional task 4）。
- 审计轮 a=6（2026-09-05，M-04 终审 + 全任务终验）：复验全部 M-04 验收命令通过（check、build + derive 入三个 d.ts、rg 11/6/24/1、`npm test` 234 文件 / 2769 通过 / 66 skip / 0 失败、定向 597 全绿且 13 用例无 skip）。D1 修正对抗性复核：读者枚举逐字段对照 `migration.ts:1010–1117`（manifest 读 controller 图 + storage schema，注册表零引用）与 `Interaction.ts:211`（parse round-trip），三方一致（知识文档 ↔ CHANGELOG:28 ↔ 设计 §1.1）；同类扫描零残留（`utils.ts:80` 为信任边界注释，非读者枚举，不构成同类）；合同五条与 `teardown` 对照表未被扩写（destroy 链 / `Controller.teardown` / `getBoundStateName` 逐点对照代码一致）。受影响里程碑复验：storage 846、migration+review 163、真实 PG 48 全绿。生产 diff 逐项复核（汇合点返回值、R1–R8 derive、闭包纯值捕获、`create()` 去重与校验原样）。无实现缺陷、无验证缺口。**M-04 `待审`→`已完成`；四个里程碑全部关闭。** 最终核验（Task 要求 1–5 逐项 + 基础测试 + 真实 PG）全部通过，`status: 已完成`，任务终止。

## 任务终止总结（a=6，2026-09-05）

- **终止状态和原因**：`已完成`。四个里程碑（M-01 红色基线、M-02 core `derive`、M-03 登记点汇合 + 闭包切断、M-04 合同文档 + CHANGELOG + 维度回填）全部经独立审计关闭，最终核验（Task 要求 1–5 逐项 + 项目基础测试 + 真实 PostgreSQL）全部通过。
- **设计轮数**：1 轮裁决（d=1/15，评审「通过」，采纳需要复审问题 0 类）。**实现轮数**：6 轮（k=1 M-01 首版、k=2 M-01 修订、k=3 M-02、k=4 M-03、k=5 M-04 首版、k=6 M-04 D1 修正）；审计轮 6 轮（a=1..a=6），预算 6/20。
- **各里程碑最终状态**：M-01 已完成、M-02 已完成、M-03 已完成、M-04 已完成。
- **设计阶段采纳的问题类别**：0 类需要复审问题（d=1 裁决轮独立复核承重主张后直接通过）；3 项评审实现注意事项被采纳为实现约束（WeakRef 形态、migration 轨两批计量、`typeof` 瑕疵不修复、allowlist 纳入 drivers——共 4 项编号 2/3/4/5）。
- **审计发现的实现缺陷数与验证缺口数**：实现缺陷 4 处（a=1：M-01 per-site 清查不可满足 + R3 无判别力 + R8 恒空，同域 3 项；a=5：M-04 知识文档读者枚举事实错误 1 项）；验证缺口 2 处（a=2：post-destroy 观察放过截断式伪修复，审计侧直接加强；a=3：allowlist 文件级判定放行白名单文件内新增站点，审计侧直接加强）。全部闭合。
- **各里程碑 reopen**：M-01 reopen 1 次（领域 `per-site 覆盖断言（判别力与可满足性）`，a=1，k=2 修复、a=2 关闭）；M-02 reopen 0；M-03 reopen 0；M-04 reopen 1 次（领域 `文档事实准确性（stringifyAllInstances 读者枚举与代码/设计/CHANGELOG 矛盾）`，a=5，k=6 修复、a=6 关闭）。无领域第二次 reopen、无里程碑第三次 reopen，**未触发收敛模式**（全程 `normal`，无 Convergence Note）。
- **自动调整里程碑的次数和原因**：0 次结构调整（k=2 在实现轮内修正设计 §3.5 第 4 项 R8 触发前提与红色基线数字，属设计事实订正，非里程碑拆分/合并；总预算 N 不变）。
- **人工介入**：0。
- **预算**：设计 1/15、实现 6/20，均满足。无未完成阻塞。
- **协议改进建议**（最多三项）：
  1. **红基线里程碑的用例形态应在设计模板中前置于实现**：M-01 三项缺陷（清查不可满足 / 断言无判别力 / 触发形态前提错误）共同根因是「断言在修复后是否可满足、在伪修复下是否可判别」没有实现前检查。建议任务模板对「先红后绿」类里程碑加一步：实现前先写出「修复后应绿」的具体数字与「伪修复下应红」的具体形态（证据：a=1 三缺陷与 a=2 伪修复缺口全部属于这一类，耗 2 轮）。
  2. **文档型里程碑的验收命令应含「事实主张 ↔ 代码」对照清单**：M-04 的 D1（读者枚举与代码矛盾）在验收命令（rg 计数 + 全量测试）下不可见，靠对抗性逐句核对才发现。建议文档交付物的验收除存在性检查外，要求列出全部可证伪事实主张及其代码锚点（证据：a=5 D1 的三重矛盾只有对照代码才暴露）。
  3. **审计侧直接加强测试的注入复验流程运转良好，建议在协议中显式命名**：a=2（伪修复注入）与 a=3（allowlist 注入）都是「加强后必须注入复验判别力、且证明未修复态仍红」的双向验证，本轮两次成功区分了验证缺口与实现缺陷、避免了不必要的 reopen。建议把该双向注入复验写成审计轮标准步骤（证据：两次加强均未触发 reopen 且后续轮次无同域回归）。
