# 运行记录 — migration-state-machine-state-remap

- 2026-09-17 任务生成（cyclic-task-prompt，template-sha256: 43a33f632926）。基线 `f9f06b3`。
- d0 设计轮（Task 1 主体）：机制 A/B/C/D/E/F/G 全部在当前 HEAD 用真实 migrate+dispatch 探针定谳（A/B/C/E/F/G 为应用层可复现，D 为测试专用制造计划在 PR #51 修复形态下的唯一索引崩溃）；读者枚举完成（§1.2）；设计文档建立，6 个里程碑全部「开放」，N = 30。下一步：additional task 1 设计评审。
- d1 裁决轮：评审结论「通过」、无需要复审的问题。逐条独立复核评审事实链（分支守卫 3659、setInternal 唯一点、stateSignature 构成、validateApprovedDiff 挂点先于 dryRun/事务、审批 helper 兜底、state-only 测试仅 Custom）全部成立，未直接接受任何未经复核的结论。评审 5 条实现注意事项全部采纳并入设计（modelHash 显式排除断言、manifest 版本不升版 + 旧格式降级测试、migrationApproval helper 认识新决策、Custom 执行层用例、不变量宽口径裁定）。裁决轮新增两个核实事实并入设计：ComputationManifest 不存原始 args（检测数据源修正）、property StateMachine takeover 今日即整体拒绝（风险 3 关闭）；migration.spec.ts 复跑 90/90；本机 PG 17.6 可用。设计通过（d=1，无采纳问题），status 转实现中，N = 30。下一步：additional task 3 实现轮，从 M-01 开始。
- 实现轮 1（k=1/30）：完成 M-01 检测层——`ComputationManifest.stateGraph` 字段（modelHash 排除、旧格式降级）、`detected.stateGraphChanged`、`state-graph-mapping` requirement/decision 与 `validateApprovedDiff` 覆盖/合法性校验；新 spec 10 用例、migration.spec.ts 90/90、三个 fuzz 套件 132/132、check/build 干净；migrationApproval 与 postgresqlMigration 本地 helper 认识新 kind。M-01 转待审。下一步：additional task 4 审计轮。
- 审计轮 1：M-01 通过。验收命令全部独立复验（10/90/132/check/build）并加跑同类消费者（本地审批 helper 67/67、真实 PG postgresqlMigration 7/7 + postgresqlIdConsistency 2/2）。实现缺陷 0。验证缺口 1：modelHash 排除断言两边同由被测代码计算，删除排除后仍通过（缺陷注入证实）；审计轮直接补金值断言（pre-M-01 代码对 setupV1 的 modelHash，跨进程确定）并双向验证判别力后完全还原注入。M-01 待审→已完成（reopen 0，未触发收敛）。下一步：additional task 3 实现轮，M-02 红灯基线。
- 实现轮 2（k=2/30）：完成 M-02 映射施加——`MigrationScheduler.run` 分支重写（触发面 = 批准的 state-graph-mapping 决策存在或 `item.rebuildState`，机制 B 的 `rebuildState=false` 计划项由此覆盖）+ `applyStateGraphMapping`（逐记录/全局 `setInternal`；合法名不动、null → `handle.initialState.name`；先映射后输出重建）+ `getStateGraphMappingDecision`（以 `computationType === StateMachine` 判定，Custom 不接管）。红灯基线先行：4 执行用例在实现前全部失败于旧名残留（B `approved≠accepted`、A `pending≠queued`、G `archived≠pending`、可区分用例），M-01 层 10 用例与 Custom 对照保持绿——复现精确锚定映射缺失。转绿后全量验收：新 spec 15/15、migration.spec.ts 90/90、三 fuzz 套件 132/132、check/build 干净。两处测试事实修正（非实现缺陷）：event rebuild handler 合约为 `({controller, dataContext, record})`；交互必须 `Interaction.create` 否则 `_Interaction_` 不进 schema。M-02 转待审。下一步：additional task 4 审计轮。
- 审计轮 2：M-02 待审→开放（实现缺陷 D-1，reopen 1，领域「迁移决策组合一致性」，未触发收敛）。验收命令全部独立复验（15/90/132/check/build + 审计加跑真实 PG postgresqlMigration 7/7、同类消费者 67/67）。D-1（探针实证，PGLite）：审批同时给出 state-graph-mapping 决策与 computation 决策 `unchanged` 时，`getChangedComputationsFromApprovedDiff` 不播种计划 → 调度器映射分支不可达 → migrate 成功但 `currentState` 停留旧名、后续 dispatch 静默无效（机制 B 经审批面复现）；违反设计 §1.2 对该读者的「扩展」要求。同类组合面逐一实测：changed+mapping、state-only+mapping 正确，unrebuildable+mapping fail-fast 安全，唯 unchanged+mapping 缺陷。缺陷注入（短路 applyStateGraphMapping）证实既有 15 用例判别力充分（4 failed/11 passed，已完全还原）——D-1 是实现缺陷不是验证缺口。修正指向 validateApprovedDiff 拒绝矛盾组合（推荐）或播种扩展；next-action 已写明。另记 M-06 注意事项：升级窗口 × 初始态改名子格产物是合法名，M-04 合法性扫描兜不住，文档须列两步迁移/baseline 出路。下一步：additional task 3 实现轮修复 D-1。
- 实现轮 3（k=3/30）：修复 D-1（unchanged/unrebuildable 计算决策 × 已批准 state-graph-mapping 的矛盾组合）。红灯先行：新增 property/global 两个组合面用例在修复前 2 failed（migrate 成功、映射静默丢弃），state-only 边界用例（防过度拒绝）修复前即绿。修复按审计推荐方案 1：validateApprovedDiff 的 state-graph-mapping 校验块追加一致性检查——同 dataContext 的 computation 决策为 unchanged 或 unrebuildable 时 fail-fast（前者是 D-1 本体，后者是同类收口：同样不播种 rebuild 计划，今日仅由 blocking 兜住，纳入后不依赖 blocking 顺序）；dryRun 同样触发。验收：新 spec 18/18、migration.spec.ts 90/90、三 fuzz 套件 132/132、check/build 干净、真实 PG postgresqlMigration 7/7。M-02 重新转待审。下一步：additional task 4 审计轮。
- 审计轮 3：M-02 待审→已完成（实现缺陷 0，reopen 维持 1，未触发收敛）。验收全部独立复验（18/90/132/check/build + 真实 PG postgresqlMigration 7/7、postgresqlIdConsistency 2/2，PG 17.6）。D-1 修复核验正确：矛盾校验落在 validateApprovedDiff（审计推荐方案 1），unchanged/unrebuildable 同根因收口——dryRun 在 blocking 前早退，unrebuildable 若不纳入则 dryRun 静默返回丢弃映射的计划（注入实证）。组合空间同类枚举：computation 决策缺失由既有 Missing-decision 循环闭合（stateGraph 变化必使 argsSignature 变化，源码推演）；初始态换名（removed=[]）探针实测正确；constructor 状态名探针证实声明面即被 TransitionFinder 拒绝、`value in mapping` 原型链暴露今日不可达（留 M-03 防御备注）。2 个验证缺口审计轮直接闭合并缺陷注入双向验证后完全还原：机制 A 的 global 旧初始名子格（原用例 global 停在合法名、旧初始名映射从未被执行——短路 global 分支时原用例放过，补强后 2 failed 捕获）、unrebuildable dryRun 断言（移除该词后 1 failed 捕获）；还原后 18/18。下一步：additional task 3 实现轮，从 M-03（状态所有权与 rebuildStateDefaults 边界，含 Transform 反例）开始。
- 实现轮 4（k=4/30）：完成 M-03 状态所有权与 rebuildStateDefaults 边界。红灯先行：Transform 双真计划复现唯一索引崩溃（setInternal('') 键坍缩）、聚合 state-only 复现 item state 清零，Custom 对照保持绿——复现锚定所有权缺失。实现：handle 静态声明 ownsStateOnRebuild（Transform、聚合双模板基类、RealTime 双 handle），MigrationScheduler.run 状态重建分支三路分流（映射决策 → 输出所有权跳过重置 → rebuildStateDefaults），新增 stateOwnedByOutputPath 读取声明。验收：新 spec 21/21、migration.spec.ts 90/90、三 fuzz 套件 132/132、check/build 干净、全 runtime 1335 passed + storage 846 passed、真实 PG postgresqlMigration 7/7。M-03 转待审。下一步：additional task 4 审计轮。
- 审计轮 4：M-03 待审→已完成（实现缺陷 0，reopen 维持 0，未触发收敛）。验收全部独立复验（21→补强后 23/90/132/check/build + 真实 PG postgresqlMigration 7/7，PG 17.6）。所有权实现核对：三路分流与设计 §3.2 一致；computationById 返回 handle 实例故 constructor 静态查找正确；全部 createState handle 家族枚举——聚合 12 子类经双基类继承全覆盖、Transform/RealTime 显式声明、StateMachine/Custom 正确不声明（映射/重置各归其主）。2 个验证缺口审计轮直接闭合并缺陷注入双向验证后完全还原：RealTime 所有权子格（实现轮声明无测试覆盖，property 级 state-only 用例补上，注入 → 1 failed）、Transform state-only 子格（「跳过重置依赖 rebuildOutput」错误形态可全过原用例，no-op 用例补上，注入条件路由 → 3 failed）。四轮注入（Transform/聚合/RealTime 声明 + 条件路由）全部还原后 23/23。防御性备注（applyStateGraphMapping 的 value in mapping → Object.hasOwn，今日不可达）移交 M-04。下一步：additional task 3 实现轮，从 M-04（合法性不变量，Task 要求 3）开始。
- 实现轮 5（k=5/30）：完成 M-04 合法性不变量。红灯先行：N1/N2/N3/N4 四负向用例（升级窗口 property/global、探针 F 迁移路径版 ghost、rebuildOutput-only 宽口径）实现前全部失败于「migrate 成功、非法名静默存活」，N0/P5 前置即绿——复现锚定不变量缺失。实现：assertPersistedStatesLegal（handle 静态 computationType 判定 StateMachine、args.states[].name 合法集合、record 单列扫描/global 读值、MigrationError 含 dataContext+非法值+合法集合），挂载顺序「映射施加 → 不变量 → 输出重建」，宽口径触发面 rebuildState||rebuildOutput；Object.hasOwn 收口 applyStateGraphMapping（审计轮 4 移交项）。验收：新 spec 29/29、migration.spec 90/90、三 fuzz 套件 132/132、check/build 干净、真实 PG postgresqlMigration 7/7。M-04 转待审。测试工程注意：迁移意外成功形态下 expect().rejects 序列化整个 MigrationPlan 会 OOM，断言用 try/catch+String(error)；StateMachine computeTarget 的 TS 断言变体不改变函数哈希（转译擦除），宽口径用例需运行时文本变体（String 包装）。下一步：additional task 4 审计轮。
- 审计轮 5：M-04 待审→开放（实现缺陷 D-2，reopen 1，领域「不变量触发面完整性」，未触发收敛）。验收命令全部独立复验通过（29/90/132/check/build、真实 PG 7/7）——但对抗性探针（PGLite，已删除）发现：state-only 计划项（rebuildState=true, rebuildOutput=false）在 run() 的 if (!item.rebuildOutput) continue（migration.ts:3885）处跳出循环，3896 行的 assertPersistedStatesLegal 对它不可达——设计 §3.4 宽口径的 rebuildState 单真半边未实现。探针实证：state-only 决策 + 正确映射 + ghost 记录 → migrate 成功（error undefined）、映射照常施加（approved→accepted）、ghost 穿过迁移存活、迁移后 dispatch 静默无效（error undefined、状态不变）——正是本任务要消灭的失败形态；state-only+mapping 是 M-02 D-1 boundary 钉住的合法审批组合，路径真实可达。既有 29 用例的交集格（state-only × 图外名）无覆盖：N1–N4 全为 rebuildOutput=true 计划，D-1 boundary 数据全合法。同类枚举定谳：rebuildOutput=false 当且仅当 state-only 决策项（changed 进 outputChangedIds、依赖传播项 !seed=true、新增项 !old=true），D-2 是唯一漏扫路径；property/global 走同一 continue 同被击中。修正指向：扫描调用移到 3885 continue 之前（保持映射→不变量→输出重建顺序）；红灯骨架与含无映射决策的升级窗口变体已写入审计文件 §2.1。M-01/M-03 维持已完成（复验全绿）；M-02 映射施加在 state-only 项上行为正确（探针中映射生效），缺陷只在扫描缺失。下一步：additional task 3 实现轮修复 D-2。
- 实现轮 6（k=6/30）：修复 D-2（state-only 计划项绕过合法性扫描）。红灯先行：N6（state-only + 正确映射 + ghost，D-1 boundary 同款合法组合）与 N7（升级窗口无映射变体：state-only 决策 + ghost）修复前均失败于 migrate 成功、ghost 存活（与审计探针输出一致），既有 29 用例保持绿。修复为 run() 控制流重排：映射施加独立提前 → 宽口径扫描（rebuildState||rebuildOutput，含 state-only 项）→ 状态重建分支（所有权/重置）→ continue/输出重建。落位采用「映射→不变量→重置」而非审计字面的「重置之后」：rebuildStateDefaults 的 defaultValue 是新初始态名，重置在扫描前会把图外名洗白（机制 C 的无差别重置），N7 格将永远拦不住——审计 §2.1 的完成条件（无映射变体必须被拦截）优先于括号内的顺序表述；§3.4「先映射后检查」的顺序约束不变。副作用界定：升级窗口×初始态改名×changed/state-only 复合格从「重置洗白、迁移成功」变为 fail-fast（§3.4 写明的升级窗口语义，审计轮 2 已记入 M-06 注意事项；既有用例无此格）。验收：新 spec 31/31（含 D-1 boundary 不过度拒绝与 P5 成本边界保持绿）、migration.spec 90/90、三 fuzz 套件 132/132、check/build 干净、全 runtime 1345 passed/58 skipped、真实 PG postgresqlMigration 7/7。M-04 重新转待审。下一步：additional task 4 审计轮。
- 审计轮 6：M-04 待审→已完成（实现缺陷 0，验证缺口 0，reopen 维持 1，未触发收敛）。验收全部独立复验（31/90/132/check/build、真实 PG postgresqlMigration 7/7，PG 17.6）。D-2 修复核验正确（映射→宽口径扫描→所有权/重置→continue，migration.ts:3871-3905）；实现轮对审计轮 5 字面顺序的偏离（扫描在重置之前）裁定正确且必要——重置 defaultValue 是合法名，先重置再扫描会把图外名洗白，注入 B（字面顺序形态）恰好 N7 转红证实。缺陷注入双向验证：注入 A（D-2 原形态收窄扫描条件）→ N6/N7 恰好转红（2 failed）；注入 B → N7 转红（1 failed）；注入完全还原后 31/31（stash 快照对照字节级还原）。同类格枚举闭环：rebuildOutput=false 当且仅当 state-only 决策项，无剩余漏扫格；currentState 全部写点（映射/重置/输出重建）相对扫描时点逐一核验；simulate 降级与 kill-resume 事务语义无回归；M-01/M-02/M-03 回归面全绿。下一步：additional task 3 实现轮，从 M-05（回归面与真实 PostgreSQL 对照）开始。
- 实现轮 7（k=7/30）：完成 M-05 回归面与真实 PostgreSQL 对照。postgresqlMigration.spec.ts 新增独立 describe（两个独占库 _state_map_b/_state_map_a）：非初始改名（机制 B 形态，先断言 diff 的 state-graph-mapping requirement，迁移后 currentState 列与输出值双面为 accepted，未转移记录不动，Archive dispatch 生效）与初始改名（机制 A 双真形态，pending→queued 映射非无差别重置、approved 记录不动，Approve dispatch 生效）；断言锚定持久化 currentState 列本身；本地审批 helper 增 stateMappings 选项。判别力经缺陷注入双向验证：短路 applyStateGraphMapping → 恰好两新场景 2 failed、既有 7 用例不受影响，还原后 9/9。回归：四验收套件 222/222、migrationStateMachineStateGraph 31/31、check/build 干净、env-gated 全量 npm run test:postgres → 11 文件/50 tests 全通过（PG 17.6，postgresqlMigration 9/9）。M-05 转待审。下一步：additional task 4 审计轮。

- 审计轮 7：M-05 待审→已完成（实现缺陷 0，reopen 维持 0，未触发收敛）。验收全部独立复验（四套件 222/222、migrationStateMachineStateGraph 31/31、check/build、真实 PG 矩阵 50/50 + 补强后 51/51，PG 17.6）。两个正向真实 PG 场景（非初始/初始改名）判别力经审计独立注入重放（短路映射施加 → 恰好 2 failed，MD5 还原）。验证缺口 1 直接闭合：映射写与不变量 fail-fast 的回滚语义此前仅有 PGLite 证据（N2/N6），新增第三个真实 PG 负向场景（_state_map_c，ghost 腐蚀 → 事务内抛错、映射写回滚、ghost 不被洗白），注入短路 assertPersistedStatesLegal → 恰好 1 failed，还原后 10/10。同类检查：本地审批 helper 未知 kind 兜底面（r2/r3/r16 展开原样、不误转 destructive-scope）、派生库名无冲突。下一步：additional task 3 实现轮，从 M-06（文档、维度注册表与逃逸复盘，最后一个里程碑）开始。
- 实现轮 8（k=8/30）：完成 M-06 文档、维度注册表与逃逸复盘。三件交付：usage 知识库新增 22-state-machine-state-graph-migration.md（README 第 23 条登记；决策写法、六条失败信息逐字核对（含 requirementKey 尾随冒号）、两步迁移修复路径、baseline 边界、Custom/所有权/运行期边界）；WritingComputationTests.md 维度表新增「迁移期状态图变更 × 作用域 × bound state 所有权 × rebuild 标志」一行（含双面断言强制与升级窗口格）；agentspace/output/ 新增 migration-state-remap-test-blindness-retrospective.md（四盲区 + 逐防线显微镜 + 五项机制化结论 + 双侧诚实边界；对 state-only 既有用例「只用 Custom+GlobalBoundState、固化可整体重置语义」的指控经源码逐行核实）。验收：文档存在性与注册表 grep 通过；行为一致性回归全绿——migrationStateMachineStateGraph+migration 121/121、三 fuzz 套件 132/132、check/build 干净、真实 PG postgresqlMigration 10/10（含 state map 3 场景）。M-06 转待审（最后一个里程碑）。下一步：additional task 4 审计轮。
- 审计轮 8：M-06 待审→已完成（实现缺陷 0，reopen 维持 0，未触发收敛）；全部里程碑完成，最终核验通过，任务终止（status: 已完成）。验收全部独立复验（121/132/check/build、真实 PG postgresqlMigration 10/10 + 全矩阵 51/51，PG 17.6）。三件交付物逐点对照实现核验一致（失败目录六条错误信息逐字核对；修复性迁移路径经探针执行验证）。验证缺口 1 直接闭合：文档「初始态改名时旧初始名记录必须移动」的因果表述在 removed=[] 保留形状下不精确（正确行为是空映射 + 记录保持仍合法名，审计轮 3 探针已定谳），措辞修正为按删除/保留两形状分列，行为与测试零改动。

## 任务终止记录（2026-09-18）

- **终止状态和原因**：正常完成。全部 6 个里程碑`已完成`，最终核验（全部验收命令、Task 要求 1–8 逐项、项目基础测试全量）通过。
- **设计轮数 / 实现轮数**：d=1（d0 设计 + d1 裁决通过）；k=8/30（预算未超）。各里程碑最终状态：M-01–M-06 全部`已完成`。
- **设计阶段采纳的问题类别**：0 类（评审「通过」，无需要复审的问题；5 条实现注意事项全部采纳并入设计，2 个裁决轮新事实并入）。
- **审计发现的实现缺陷数 / 验证缺口数**：实现缺陷 2（D-1：unchanged × 已批准映射被静默丢弃；D-2：state-only 计划项绕过合法性扫描）+ 文档表述缺口 1（M-06 因果措辞，非产品缺陷）；验证缺口 8 个全部由审计轮直接闭合并经缺陷注入双向验证（modelHash 金值、机制 A global 子格、unrebuildable dryRun、RealTime/Transform state-only 所有权子格、真实 PG 事务回滚场景、M-06 文档措辞）。
- **各里程碑 reopen**：M-02 ×1（领域「迁移决策组合一致性」，第 3 轮关闭）；M-04 ×1（领域「不变量触发面完整性」，第 6 轮关闭）；其余 0。无同领域第二次 reopen、无里程碑第三次 reopen，收敛模式全程 normal。
- **自动调整里程碑的次数和原因**：0。
- **人工介入**：0。
- **预算**：满足（k=8 ≪ N=30；d=1 ＜ 15）。
- **协议改进建议**（最多三项）：
  1. 审计轮的修正指令应把「完成条件」与「实现手段顺序」显式分离——实现轮 6 对审计轮 5 字面顺序的必要偏离（扫描必须在重置之前，否则图外名被洗白）表明：当二者冲突时执行者只能自行裁定。证据：审计轮 6 裁定偏离正确，注入 B（字面顺序形态）恰好使 N7 失守。
  2. 文档类里程碑的验收应默认包含「逐句因果表述对照真实行为」的核验义务，而不只是存在性 + grep——本轮发现的措辞缺口（初始改名保留形状）存在于已全绿的行为之上。证据：审计轮 8 §3。
  3. 「验证缺口由审计轮直接闭合」的机制运转良好（8 次闭合均不退回实现轮且经注入双向验证），建议在协议中显式承认这是常态路径而非例外，避免审计轮为区分缺陷/缺口付出重复论证成本。证据：审计轮 1/3/4/5(部分)/7/8。
